{
  "name": "cowx",
  "version": "0.1.0",
  "description": "COWX — Colorado Weather: aggregated air quality and weather data",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch": "node scripts/fetch/index.js",
    "fetch:data": "node scripts/fetch/index.js",
    "test": "node --test tests/**/*.test.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "validate:locations": "node scripts/validate-locations.js",
    "update": "pnpm update && pnpm dedupe",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,mjs,cjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml,html}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "globals": "^16.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.5"
  },
  "packageManager": "pnpm@10.34.3"
}
